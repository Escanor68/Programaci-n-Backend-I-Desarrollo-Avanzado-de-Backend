/**
 * Exportar configuración de Socket.io
 * Punto de entrada para todos los handlers de Socket.io
 */

const configureSocketHandlers = require('./socketHandlers');

module.exports = configureSocketHandlers;
